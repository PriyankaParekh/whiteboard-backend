version: "3.8"

services:
  api:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - .:/app
      - /node_modules

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
